# 프로젝트 결정사항 및 진행 기록

방제지리정보 주소 지오코딩 도구(정적 파일, `dgss-geocode-tool`) 관련 대화에서 정한 내용 전체와 진행 상황 정리.

---

## 1. 프로젝트 성격

- **WebGISPlatform과 별개의 독립 프로젝트.** WebGISPlatform 저장소에 소스를 넣지 않는다.
- 빌드 과정 없음. `index.html`/`app.js`/`style.css` + `src/*.js` 정적 파일만으로 동작.
- 실행 환경: 인터넷 되는 PC의 별도 정적 도구로 유지 (서버 통합 안 함). GitHub Pages로 배포.
- **Git 브랜치 운영**: `claude` 브랜치에만 커밋·push. `main`으로의 병합은 사용자가 GitHub에서 직접 PR로 진행.

## 2. "대체주소" 기능 정의

- **정의**: 지명형 주소(번지 없는 공원·시설명 등)를 검색 가능한 실제 주소로 치환하는 것.
- **대체주소 컬럼이 채워지는 조건**: 지명형이 실제 주소로 치환된 경우에만. 이미 유효한 주소로 주소검색 매칭된 행은 빈칸.
  - (이후 병렬 세션에서 `_작업` 시트 구조에 맞춰 컬럼명이 `대체주소`→`지오코딩 안된 주소 보충`(레거시 이름 유지, 내용은 실제 확정 주소)으로 재조정됨)
- **오답 방지**: 검색 범위를 시군구로 제한(지역 게이트). 관할 밖 후보는 버리지 않고 검수 목록에 "관할 밖" 배지로 표시.

## 3. 지오코딩 엔진

- 카카오 + VWorld Open API **병행**. 어댑터 구조로 분리.
- VWorld는 `crs=EPSG:2097`로 직접 요청(좌표 변환 불필요), 카카오는 WGS84→2097 proj4 변환.
- **VWorld 키**: 사용자가 직접 발급, 직접 `index.html`의 `VWORLD_API_KEY`에 입력. Claude는 API 키/토큰류를 대신 입력하지 않는다는 원칙으로 코드에 실키를 넣지 않았음.
- **약관 확인**: VWorld/카카오의 "결과 저장 금지" 조항 문제는 사용자가 업무용으로 문제없다고 확인함.
- 사고: 사용자가 GitHub에서 `PLACEHOLDER` 상수 자리에 실키를 잘못 넣어 공개 커밋 이력에 노출된 적 있음 → 폐기 후 재발급 권고함.

## 4. 대체주소 사전

- 한 번 확정한 치환을 누적해 다음 실행 때 API 호출 없이 재사용.
- **좌표는 WGS84 경위도만 저장**, X/Y는 사용 시점 좌표계로 재계산 (사전 생성 시점과 다른 좌표계를 골라도 어긋나지 않게).
- 엑셀로 내보내기/불러오기 가능. 결과 워크북에도 사본 저장.

## 5. 다중 시트

- 여러 시트를 체크박스로 선택해 한 번에 처리, 검수는 한 화면에서 통합.
- 컬럼은 헤더 **이름**으로 시트 간 매칭 (헤더 같은 시트는 매핑 1회로 전체 적용).
- 동일 주소 확정 시 **시트를 넘나들며** 일괄 반영.

## 6. 결과 워크북 구조

- **원본 시트는 절대 건드리지 않는다** (값·구조 그대로).
- `A` / `A_완료` / `B` / `B_완료` 순서로 원본 바로 뒤에 결과 시트 삽입.
- **컬럼 삽입 위치**: 결과 컬럼을 원본 뒤에 단순 추가하지 않고, **주소 컬럼(지번/도로명) 바로 뒤에 끼워넣고** 나머지 원본 컬럼(상세주소·기타 등)은 오른쪽으로 민다. (실제 사용자가 수작업하던 `_작업` 시트 레이아웃과 대조해서 확인한 요구사항)
- 엑셀 시트명 31자 제한 처리, 열 너비/병합은 위치가 바뀌므로 복사하지 않음.

## 7. 중복 제거

판정 기준 3가지 조건을 정리하면 **"확정 주소가 같은가" 하나**로 귀결됨:
- 시설명 같음 + 주소 같음 → 동일 시설 (병합)
- 시설명 같음 + 주소 다름 → 별개 시설 (유지)
- 시설명 다름 + 주소 같음 → 동일 시설 (병합)

→ 시설명은 최종 판정에 영향 없음. 대체주소 > 지번결과 > 도로명결과 > 원본 소재지 순으로 확정되는 주소 하나로만 그룹핑.

- 그룹 안에서 **좌표 확정된 행 우선**, 그다음 첫 번째 행만 유지.
- 적용 범위: **시트 단위**(다른 시트 것과 합쳐서 지우지 않음).
- 원본 시트는 안 건드리고 `_완료` 시트에만 반영.
- `중복제거로그` 시트로 감사 추적(어떤 행이 남고 어떤 행이 지워졌는지).

## 8. 좌표계

- **기본값 EPSG:2097** (WebGISPlatform 표준으로 알려졌던 5181에서, 사용자가 실제 작업 시 쓰는 값인 2097로 확정).
- 드롭다운 표시와 내부 기본값 동기화 버그가 있었으나 병렬 세션에서 수정됨.

## 9. 오류 처리

- **연속 API 오류 차단기**: 10회 연속 오류(성공/무결과 한 번도 안 섞임) 시 전체 중단 + 명시 안내. 쿼터 소진이 "검색 결과 없음"으로 위장되는 것 방지.
- **라이브러리 로드 실패 감지**: XLSX/proj4/카카오 SDK/자체 모듈 중 하나라도 안 올라오면 조용히 죽지 않고 화면에 안내 + 파일 입력 차단.

## 10. 주소 파싱 버그 수정 (실사용/실데이터 기반, 총 7종+)

1. 도로명+번지 공백없이 결합 (`곳산길157-24`)
2. 읍면동+번지 공백없이 결합 (`구산동1071`, `덕이동산207` 산+번지 포함)
3. 시군구-주소 사이 낀 무관한 단어 스킵 (`민원 킨텍스로240`)
4. 시도 축약형 인식 (`경기` = `경기도`)
5. 괄호 참고메모 무시 (`대산로58(민원)`)
6. 번지 확정 후 남은 텍스트 무시 (`대화동1974-5 유충`) — route() 판정 로직 개선
7. 번지 뒤 "번지"자, 번지+접미어 붙여쓰기 (`구산동930번지`, `일청로12번길28주변`)
8. (병렬 세션 추가분) 번지 뒤 시설명 붙어쓰기, 콤마 다중 번지, 문자열 중간 참고메모 괄호

**검증 방법**: 사용자가 첨부한 실제 엑셀(11개 시트, 실주소 2177건)을 zip 구조로 직접 파싱해 전수 스캔 → 수정 전 지명형(place) 오분류 767건(35%) → 수정 후 488건(22.4%). 남은 488건은 검증 결과 전부 진짜 지명(공원명·학교명·아파트단지·역 출구 등)으로 정상.

## 11. 실제 데이터 비교로 발견한 사실

- 사용자가 첨부한 엑셀에 `취약시설,취약지_작업`이라는 **수작업 시트**가 이미 존재 — 우리 도구 출력이 아니라 예전 버전 도구 결과를 사람이 손으로 재배치·재구성해 계속 써온 것으로 확인됨. 컬럼명(`locx`, `지오코딩 안된 주소 보충`)이 현재 도구보다 훨씬 예전 버전 것이었음.
- 이 비교를 근거로 §6(컬럼 삽입 위치)과 컬럼명 재조정이 이뤄짐.

---

# 진행 상황

## 완료
전체가 `claude` 브랜치에 커밋·push됨 (`main`은 사용자가 직접 병합 예정).
- 위 1~11 전체 구현 및 회귀 테스트 (address/gate/dictionary 순수 모듈 테스트, 최소 100건 이상 통과)

## 남은 것
- [ ] **실제 브라우저로 전체 흐름 검증 안 됨** — 지금까지는 전부 Node 시뮬레이션/단위 테스트로만 확인. 실제 엑셀을 도구에 올려서 지오코딩→검수→다운로드까지 직접 눌러봐야 함
- [ ] VWorld 실키 테스트 — 응답 필드명이 문서 요약 기반 추정치라 실제 호출 결과로 확인 필요 (필드 불일치 시 콘솔에 원본 응답 로그 남도록 이미 안전장치는 넣어둠)
- [ ] `main` 브랜치 병합 (사용자가 GitHub에서 직접 PR 머지)
- [ ] 의도적으로 자동화 안 한 것: 쉼표로 도로명+시설명이 섞인 복잡한 자유서술형 주소는 사람 검수용으로 남겨둠 (예: `고양대로672번길15-15, 미림빌라`)
