# 변경 이력

빌드/버전 관리 도구 없는 정적 파일 프로젝트라 SemVer 태그 대신 날짜 기준으로 기록한다.
각 항목은 실제 데이터(`방제지리정보_데이터입력양식.xlsx`)를 전수 검증해서 발견한 문제를 고친 것이다.

## 2026-07-31 — 실데이터 검증 기반 파싱/구조/좌표계 정리

### 주소 파싱 (`src/address.js`)

- **번지 뒤 시설명 붙어쓰기 인식** — `대화동1449-1송포공원`, `경의로790한산천`처럼 번지 바로 뒤에
  공백 없이 시설명이 붙으면 통째로 지명형(장소검색)으로 빠지던 문제를 고쳤다. 엄격한 기존
  정규식을 1순위로 유지하고, 실패했을 때만 시설명 허용 완화형을 2순위로 시도하도록 해서
  `산덕로24번길12-2`처럼 이미 정상 동작하던 케이스의 회귀를 막았다.
- **콤마로 나열된 다중 번지 인식** — `주엽동3,142`, `일산동1042,1043`처럼 한 필지에 번지가
  여러 개 나열된 주소가 전혀 파싱되지 않고 지명형으로 빠지던 문제를 고쳤다. 첫 번째 번지로
  주소검색 경로를 잡고, 검색 쿼리에서도 두 번째 이후 번지를 제거한 변형을 추가했다
  (`addressVariants`).
- **참고메모 괄호가 문자열 중간에 낀 경우 대응** — `종합운동장주변(민원)대화동2325-3`처럼
  괄호가 문자열 끝이 아니라 중간에 있으면 전혀 제거되지 않던 문제를 고쳤다. 삭제 대신
  공백으로 치환해서, 괄호 앞뒤 숫자가 붙어버려 번지가 오염되는 걸 막았다
  (`덕이로220-10(10번홪자)5회` → `220-105` 로 깨지는 문제 방지).
- 위 세 가지 모두 실제 엑셀 11개 시트 스캔에서 반복 확인된 패턴이며, 각 패턴을 실주소 그대로
  회귀 테스트에 고정했다 (`tests/address.test.js`, 총 142건 통과).

### 결과 컬럼 구조 (`app.js`)

- `_완료` 시트 컬럼을 기존 수작업 `_작업` 시트와 **완전히 동일한 구조**로 맞췄다.
  - 컬럼명 변경: `대체주소` → `지오코딩 안된 주소 보충` (예전 컬럼명 재사용. 다만 내용은
    검색 쿼리가 아니라 실제로 확정된 주소를 넣도록 유지 — 예전 버전의 결함은 되살리지 않음)
  - 제거: `경도(WGS84)`, `위도(WGS84)`, `매칭방식`, `검색어` 컬럼 생성 안 함
  - 추가: `locx` (예전 시트의 빈 컬럼, 자리만 맞춤)
  - 컬럼 삽입 순서: `소재지 → locx → 지오코딩 안된 주소 보충 → x → y → 지번 → 도로명`

### 좌표계

- 기본 좌표계를 **EPSG:2097**로 재확인. `targetCrs` 기본값은 이미 2097이었지만, 좌표계
  드롭다운(`populateCrsSelect`)이 화면 표시 값을 `targetCrs`와 동기화하지 않아 항상 목록
  첫 항목인 **5181이 화면에 보이던 버그**를 고쳤다. 드롭다운을 건드리지 않고 바로 시작하면
  이 값이 그대로 적용되므로(`targetCrs = $('crsSelect').value`), 실제로는 5181로 처리될
  위험이 있었다.
- `DESIGN.md`, `README.md`에 남아있던 "EPSG:5181 고정/표준" 서술을 EPSG:2097로 정정.

### 알려진 미해결 항목

- 괄호 중간 삽입 케이스 중 두 개의 서로 다른 지명이 그냥 이어붙은 경우(예:
  `종합운동장주변(민원)보조경기장`)는 여전히 지명형으로 남는다 — 정규식으로 억지로 분리하면
  다른 유효한 주소를 깨뜨릴 위험이 있어 보류. 검색 키워드만 정리(괄호 제거)했다.
- 이런 새로운 패턴은 앞으로도 계속 나올 수 있다. 다만 못 잡아도 조용히 틀린 좌표가 들어가는
  게 아니라 지역 게이트 + 검수 목록을 거치므로, 한 번 수동 확정하면 대체주소 사전에 남아
  다음 회차부터는 자동 처리된다.
