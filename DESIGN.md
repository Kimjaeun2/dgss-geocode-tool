# 엑셀 주소 지오코딩 도구 재설계

**작성일**: 2026-07-29
**대상**: `geocode-tool-static` (방제지리정보 주소 지오코딩 도구)
**상태**: 설계 확정, 구현 계획 대기

---

## 1. 배경

엑셀에 담긴 대상지 목록에서 주소를 읽어 좌표(EPSG:2097)를 채워 넣는 도구가 이미 정적 파일(`index.html` / `app.js` / `style.css`) 형태로 존재한다. 브라우저에서 카카오맵 JS SDK를 호출해 지오코딩하고, 실패한 건은 담당자가 지도에서 직접 보정한 뒤 엑셀로 내려받는다.

문제는 **번지·건물번호가 없는 지명형 주소**다. `'경기도 고양시 일산서구 한뫼공원주변'`, `'... 멱절마을 한류천'` 같은 주소는 주소검색으로 잡히지 않아 장소검색으로 폴백하는데, 이 경로의 정확도가 낮고 오답이 조용히 섞여 들어간다.

이 문서는 그 원인을 코드 수준에서 규명하고, **지명형 주소를 지오코딩 가능한 실제 주소("대체주소")로 치환하는 기능**을 중심으로 도구를 재설계한다.

---

## 2. 범위

### 포함
- 지명형 주소 → 지오코딩 가능한 실제 지번/도로명 주소로 치환하고 엑셀에 기록
- 지역(시·군·구) 기준 검색 범위 제한으로 오답 차단
- 확정된 치환 결과를 **대체주소 사전**으로 누적·재사용
- 여러 시트를 한 번에 처리하고 통합 검수
- 지오코딩 엔진을 어댑터로 분리 (카카오 / VWorld 병행)

### 제외
- 주소 칸이 아예 비어 있는 행을 다른 단서로 채우는 기능
- 필지를 못 찾았을 때 읍·면·동 중심점 같은 근사 좌표를 부여하는 기능
- WebGISPlatform 서버·DB 연동 (도구는 별도 정적 파일로 유지)

---

## 3. 현행 도구의 결함

구현 시 반드시 해소해야 할 항목이다. 모든 줄 번호는 첨부된 `app.js` 기준.

### 3.1 지명형 판정이 틀린다 — `app.js:270`

```js
function isLandmarkAddress(addr) {
  const tail = addr.split(' ').pop() || '';
  return !/\d/.test(tail);
}
```

마지막 토큰에 숫자가 없으면 지명형으로 보고 주소검색을 건너뛴다.

- `'경기도 고양시 일산서구 대화동'` → tail=`'대화동'` → **지명형으로 오판.** 주소검색이면 정확히 잡혔을 건이 장소검색 경로로 빠진다.
- `'... 제1공원'` → tail에 숫자 있음 → 주소검색 시도 → 반드시 실패 → 호출 낭비.

### 3.2 주소 변형이 조합되지 않는다 — `app.js:76-84`

```js
push(addr.replace(/\s*(주변|일대|인근|부근|옆|앞|뒤)\s*$/, ''));
const parts = addr.split(' ');
if (parts.length >= 3) push(parts.slice(-2).join(' '));
```

두 변형이 모두 원본 `addr`을 기준으로 만들어져 서로 조합되지 않는다.
`'경기도 고양시 일산서구 한뫼공원주변'`의 변형은 다음 셋뿐이다.

1. `'경기도 고양시 일산서구 한뫼공원주변'` (원본)
2. `'경기도 고양시 일산서구 한뫼공원'` (접미어 제거)
3. `'일산서구 한뫼공원주변'` (뒤 2토큰)

**가장 잘 검색될 `'일산서구 한뫼공원'`(접미어 제거 + 축약)은 생성되지 않는다.**

또한 76행 주석은 결과가 `'고양시 한뫼공원'`이라고 적혀 있으나 `slice(-2)`이므로 실제로는 `'일산서구 한뫼공원'`이다. 주석과 코드가 불일치한다.

### 3.3 지역 제한 없는 자동 채택 — `app.js:315`

```js
if (r.data.length === 1) { /* 자동 확정 */ }
```

`keywordVariants`가 시·도/시·군·구 접두어를 떼고 검색하므로, 전국에서 동명 장소가 딱 1건 잡히면 **아무 경고 없이 틀린 좌표가 기록된다.** 결과 엑셀만 봐서는 검증할 방법이 없다.

### 3.4 동일 주소 수동 보정이 1행에만 반영된다 — `app.js:647`

`saveCoord`는 `reviewList[activeIdx]`가 가리키는 행에만 기록한다. 같은 주소가 10행에 있으면 검수 목록에 10건이 뜨고 10번 클릭해야 한다. (`geocodeCache`는 자동 처리 단계에만 적용된다.)

### 3.5 "대체주소" 컬럼에 주소가 아니라 검색 쿼리가 들어간다 — `app.js:436`

```js
if (o.usedQuery && o.usedQuery !== o.original) row[colIdx.supplement] = o.usedQuery;
```

`지오코딩 안된 주소 보충` 컬럼에 들어가는 값은 API가 돌려준 주소가 아니라 **검색에 사용한 문자열**이다. 담당자가 확인하고 싶은 것은 "어떤 주소로 잡혔나"이지 "어떤 문자열로 검색했나"가 아니다.

### 3.6 좌표계 자동 판정이 뒤집힐 수 있다 — `app.js:328-392`

표본 주소 최대 15건을 지오코딩해 기존 X/Y와의 평균 오차로 좌표계를 고른다. 그런데 EPSG:5181과 EPSG:2097은 원점이 같고 타원체(GRS80 / Bessel)만 달라 차이가 100~200m 수준이다. 표본이 적거나 기존 좌표에 오류가 섞여 있으면 판정이 뒤집힌다.

**이 로직은 애초에 불필요하다.** 실제 사용 중인 데이터를 확인한 결과 이 도구가 다루는 좌표계는 **EPSG:2097**이다. (아래는 최초 설계 당시 WebGISPlatform의 SQL 코드를 근거로 EPSG:5181로 잘못 추정했던 기록이며, 실데이터 확인 후 정정한다. WebGISPlatform SQL이 실제로 5181 SRID를 태그하고 있다면, 이 도구가 다루는 좌표값과 시스템에 저장되는 SRID 표기가 서로 다를 가능성이 있다는 뜻이므로 — 이 도구 자체의 문제는 아니지만 — WebGISPlatform 쪽에서 한 번 확인이 필요할 수 있다.)

- `src/main/resources/.../component/map/MapAnalyse_SQL_Postgres.xml:171` 외 다수에서 `ST_SetSRID(..., 5181)` 사용
- `Invest_SQL_Postgres.xml`, `Manage_SQL_Postgres.xml`, `PipeWash_SQL_Postgres.xml` 모두 동일

### 3.7 쿼터 소진이 "검색 결과 없음"으로 위장된다 — `app.js:256-263`

```js
async function callKakaoWithRetry(method, query, tries = 3) {
  for (let i = 0; i < tries; i++) { ... }
  return { state: 'error' };
}
```

카카오 JS SDK는 실패 원인(쿼터 초과 / 네트워크 / 서버 오류)을 구분해주지 않는다. 재시도 3회 후 `error`가 되고 호출부는 이를 `fail`로 처리한다. **쿼터가 소진되면 남은 수천 건이 전부 "검색 결과 없음"으로 기록되며, 로그만 봐서는 원인을 알 수 없다.**

### 3.8 실패 결과까지 캐시된다 — `app.js:287`

```js
geocodeCache.set(addr, result);   // status:'fail' 도 캐시
```

같은 실행 안에서 일시적 오류로 실패한 주소를 다시 시도할 여지가 없다.

### 3.9 셀 서식이 보존되지 않는다 — `app.js:680-687`

`cellStyles: true`로 읽고 `!cols` / `!merges` / `!freeze`만 되살리지만, SheetJS 커뮤니티 버전은 **스타일 쓰기를 지원하지 않는다.** 셀 색·글꼴·테두리는 저장 시 소실된다. (참고: [sheetjs#3214](https://git.sheetjs.com/sheetjs/sheetjs/issues/3214))

### 3.10 라이브러리 로드 실패가 조용히 죽는다 — `index.html:9-18`

`xlsx`, `proj4`를 cdnjs에서, 카카오 SDK를 dapi.kakao.com에서 로드한다. 사내 프록시가 차단하면 아무 안내 없이 동작이 멈춘다.

### 3.11 (참고) 카카오 JS 키가 파일에 노출되어 있다 — `index.html:18`

카카오 JavaScript 키는 도메인 검증 전제로 설계되어 공개 자체가 곧 취약점은 아니다. 다만 README가 GitHub Pages 업로드를 안내하므로, **디벨로퍼스에 등록하는 도메인을 실제 서비스 주소로 좁게 유지**해야 한다.

---

## 4. 설계 결정

브레인스토밍에서 확정된 사항이다.

| 항목 | 결정 |
|---|---|
| "대체주소"의 정의 | 지명형 주소를 지오코딩 가능한 실제 주소로 치환한 것 |
| 실행 환경 | 인터넷 되는 PC의 별도 정적 도구로 유지 (서버 통합 안 함) |
| 오답 방지 전략 | 검색 범위를 지역으로 제한해 애초에 유입 차단 |
| 지역 게이트 기준 | **시·군·구** (읍·면·동은 너무 조여서 놓침이 늘어남) |
| 관할 밖 후보 | 폐기하지 않고 검수 목록에 "관할 밖"으로 함께 표시 |
| 사전 보관 | 별도 엑셀 파일로 내보내기 / 불러오기 |
| 지오코딩 엔진 | 어댑터로 분리, 카카오 + VWorld 병행 |
| 다중 시트 | 여러 시트를 한 번에 처리하고 통합 검수 |
| 결과 워크북 | `A` / `A_완료` / `B` / `B_완료` 인터리브. 원본 시트는 변형하지 않음 |

---

## 5. 아키텍처

빌드 과정 없음을 유지한다. 파일만 늘린다.

**이 도구는 WebGISPlatform과 별개의 독립 프로젝트다.** 소스를 WebGISPlatform 저장소에 넣지 않는다. 도구 폴더 하나를 통째로 압축해 배포하며, 웹서버의 정적 폴더에 풀어 놓으면 그대로 동작한다.

```
geocode-tool/
  index.html
  style.css
  test.html              ← 열면 단위 테스트가 실행됨
  vendor/
    xlsx.full.min.js     ← CDN 대신 동봉 (프록시 차단 대비)
    proj4.js             ← 동봉
    ※ 카카오 SDK는 동봉 불가. dapi.kakao.com에서 로드해야 하며
      도메인 검증을 거치므로 로컬 사본으로 대체할 수 없다.
  src/
    app.js               ← 화면 조립, 이벤트 배선
    excel.js             ← 워크북 읽기/쓰기, 시트·컬럼 매핑
    address.js           ← 정규화, 행정구역 파싱, 변형 생성   (순수 함수)
    gate.js              ← 지역 게이트 판정                    (순수 함수)
    dictionary.js        ← 대체주소 사전 로드/조회/저장
    geocode.js           ← 폴백 체인 오케스트레이션
    providers/
      kakao.js
      vworld.js
    review.js            ← 검수 목록 + 지도 UI
```

`address.js`, `gate.js`, `dictionary.js`는 **API와 DOM에 의존하지 않는 순수 함수**로 유지한다. 로직 오류의 대부분이 이 세 파일에 몰려 있으므로, 여기만 테스트해도 회귀의 상당 부분을 막는다.

### 5.1 프로바이더 인터페이스

```js
// 모든 프로바이더가 이 형태로 반환한다.
{
  status: 'ok' | 'zero' | 'error',
  results: [{
    lon, lat,        // WGS84
    x, y,            // EPSG:2097 (프로바이더가 직접 주면 그대로, 아니면 proj4 변환)
    jibun,           // 지번주소
    road,            // 도로명주소
    placeName,       // 장소검색인 경우만
  }],
  source: 'kakao' | 'vworld' | 'dict',
  usedQuery: String,
}
```

| 프로바이더 | 메서드 | 비고 |
|---|---|---|
| `kakao` | `searchAddress` / `searchPlace` | WGS84 반환 → proj4로 2097 변환 |
| `vworld` | `getcoord(PARCEL\|ROAD)` / `searchPlace` | `crs=EPSG:2097` 지정 → **변환 불필요** |
| `dict` | `lookup` | 저장된 좌표를 그대로 반환. 네트워크 불필요 |

---

## 6. 주소 파싱

문자열 휴리스틱을 버리고 구조적으로 분해한다.

```
'경기도 고양시 일산서구 한뫼공원주변'
  → { sido: '경기도', sgg: '고양시 일산서구', emd: null,
      rest: '한뫼공원', suffix: '주변', bunji: null }

'경기도 고양시 일산서구 대화동 2600'
  → { sido: '경기도', sgg: '고양시 일산서구', emd: '대화동',
      rest: null, suffix: null, bunji: '2600' }
```

### 파싱 규칙

1. **정규화**: NBSP(` `), ` `, ` `, `　`을 일반 공백으로. 연속 공백 축약. 앞뒤 공백 제거. (현행 `normalizeAddress` 유지)
2. **시·도**: `~도` / `~특별시` / `~광역시` / `~특별자치시` / `~특별자치도`
3. **시·군·구**: `~시` / `~군` / `~구`. **2단 케이스**(`고양시 일산서구`)는 연속된 두 토큰을 묶는다.
4. **세종특별자치시**: 시·군·구 단계가 없다. 시·도만 잡고 시·군·구는 시·도와 동일하게 취급한다.
5. **읍·면·동·리**: `~읍` / `~면` / `~동` / `~가` / `~리`
6. **번지**: `\d+(-\d+)?` 또는 `산\s*\d+(-\d+)?`
7. **도로명**: `~로` / `~길` + 건물번호
8. **위치 접미어**: `주변` / `일대` / `인근` / `부근` / `옆` / `앞` / `뒤` — `rest`의 끝에서 분리
9. **잔여(`rest`)**: 위 어디에도 해당하지 않는 토큰 = 지명 후보

### 경로 판정

| 조건 | 경로 |
|---|---|
| `bunji` 또는 도로명+건물번호 있음 | 주소검색 |
| `emd`까지만 있고 `rest` 없음 | 주소검색 |
| `rest`가 있음 | **지명형** → 장소검색 |

3.1의 오판이 이 규칙으로 해소된다. `'경기도 고양시 일산서구 대화동'`은 `rest`가 없으므로 주소검색 경로로 간다.

### 변형 생성

주소검색 경로에서 실패 시 순서대로 시도한다. **조합을 모두 생성한다.**

1. 원본
2. 한글+숫자 사이 공백 삽입 (`'가좌로128'` → `'가좌로 128'`)
3. 괄호 부가정보 제거
4. 2 + 3 동시 적용

장소검색 경로의 키워드 후보는 파싱 결과를 조립해 만든다. 넓은 것부터:

1. `sgg + emd + rest` — `'고양시 일산서구 대화동 한뫼공원'`
2. `sgg + rest` — `'고양시 일산서구 한뫼공원'`
3. `rest` — `'한뫼공원'`

`suffix`는 항상 제거한 상태로 조립하므로 3.2의 조합 누락이 발생하지 않는다.

---

## 7. 지오코딩 파이프라인

```
행 1건
 │
 0) 사전 조회 (정규화된 원본주소 완전일치)
 │    └─ HIT ──► 저장된 좌표·대체주소로 확정 (매칭방식: 사전)  ※ 네트워크 없음
 │
 1) 정규화 + 파싱
 │
 2) [주소검색 경로일 때]
 │    VWorld getcoord(PARCEL) → VWorld getcoord(ROAD) → 카카오 addressSearch
 │    각 단계마다 변형 4종 순차 시도
 │    └─ 성공 ──► 확정 (매칭방식: 주소검색 / 주소검색(보정))
 │
 3) [지명형이거나 2)가 전부 실패]
 │    카카오 keywordSearch → VWorld search(PLACE)
 │    키워드 후보 3종 순차 시도
 │    │
 │    └─ 지역 게이트 적용 ─────────────────────────┐
 │         결과의 지번주소가 파싱된 sgg와 일치하는가? │
 │         YES → 관할 내 후보                        │
 │         NO  → 관할 밖 후보 (버리지 않고 보관)      │
 │       ───────────────────────────────────────────┘
 │
 │       관할 내 1건    ──► 자동 확정 (장소검색(자동))
 │       관할 내 2건 +  ──► 검수 목록 (후보 동봉)
 │       관할 내 0건    ──► 검수 목록 (사유: 관할 내 결과 없음
 │                                     + 관할 밖 후보 동봉)
 │
 4) 검수: 담당자가 후보를 고르거나 지도를 직접 클릭
      ├─ 해당 행 기록
      ├─ 동일 원본주소를 가진 **모든 시트의 모든 행**에 일괄 반영
      └─ 대체주소 사전에 등록
```

### 폴백 체인은 설정으로 바꾼다

각 단계를 UI 토글로 켜고 끌 수 있게 한다. 약관·쿼터 문제가 생겨도 코드 수정 없이 대응할 수 있고, 나중에 내부 DB 어댑터를 붙일 자리가 된다.

### 캐시

- **성공만 캐시한다.** 실패는 캐시하지 않아 재시도 여지를 남긴다 (3.8 해소)
- 캐시 키는 정규화된 원본 주소
- 새 파일/새 시트 선택 시 초기화

---

## 8. 지역 게이트

```js
// gate.js
function isInJurisdiction(resultJibun, parsedSgg) → boolean
```

**판정**: API가 돌려준 지번주소에서 시·군·구를 파싱해 원본의 `sgg`와 비교한다. 문자열 `startsWith`가 아니라 **파싱한 시·군·구끼리 비교**한다. `'고양시 일산서구'` vs `'고양시일산서구'` 같은 표기 차이를 흡수해야 한다.

**읍·면·동은 보지 않는다.** 공원·하천은 동 경계를 넘나들기 때문에 여기까지 조이면 정답을 놓친다.

**관할 밖 후보는 폐기하지 않는다.** 검수 목록에 `관할 밖` 배지를 달아 함께 보여준다. 시·군 경계에 걸친 하천이나, 주소는 A시로 적혀 있지만 실제 시설이 B시에 있는 경우를 담당자가 판단할 수 있어야 한다.

**`sgg` 파싱에 실패한 행은 게이트를 적용하지 않는다.** 대신 결과에 `게이트 미적용` 표시를 남겨 검수 우선순위를 높인다.

---

## 9. 대체주소 사전

### 스키마 (엑셀 1시트)

| 컬럼 | 설명 | 형식 |
|---|---|---|
| `원본주소` | 정규화된 원본 문자열. 조회 키 | 문자열 |
| `대체주소` | 지오코딩에 성공한 실제 주소 | 문자열 |
| `X` / `Y` | EPSG:2097 | 소수점 4자리 |
| `경도(WGS84)` / `위도(WGS84)` | | 소수점 7자리 |
| `시군구` | 게이트 검증용 | 문자열 |
| `출처` | kakao / vworld / 수동지정 | 문자열 |
| `등록일` | | `YYYY-MM-DD` |
| `비고` | 담당자 메모 | 문자열 |

예시 행(합성값):

| 원본주소 | 대체주소 | X | Y | 경도(WGS84) | 위도(WGS84) | 시군구 | 출처 | 등록일 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| 경기도 ○○시 △△구 □□공원주변 | 경기도 ○○시 △△구 ◇◇동 1000 | 200000.0000 | 550000.0000 | 127.0000000 | 37.6000000 | ○○시 △△구 | kakao | 2026-07-29 | 검수확정 |

### 조회 규칙

- **완전일치만.** 부분일치는 오답 위험이 커서 쓰지 않는다
- 히트 시 저장된 좌표를 그대로 사용한다. 대체주소를 다시 지오코딩하지 않는다 → **인터넷 없이도 사전 히트 건은 처리된다**
- 사전 파일이 없어도 도구는 정상 동작한다 (전부 miss로 처리)

### 수명주기

1. 시작 화면에서 사전 엑셀 업로드 (선택)
2. 검수에서 확정할 때마다 메모리 사전에 추가
3. 작업 종료 시 사전 엑셀 다운로드 → 공유 폴더에 갱신
4. 결과 워크북에도 `대체주소사전` 시트로 사본을 남긴다 (유실 대비)

매년 같은 대상지를 반복 처리하므로, 회차를 거듭할수록 수동 보정 건수가 줄어든다.

---

## 10. 다중 시트

### 데이터 모델 변경

```js
// 현재: 단일 시트만 가능
let aoa = [], colIdx = {}, sheetName = null;

// 변경
let sheets = [{ name, aoa, colIdx, enabled }, ...];
// reviewList 항목에 sheetName 추가
```

### 컬럼 매핑

헤더 행으로 시그니처를 만들어 **같은 헤더를 가진 시트끼리 자동 그룹화**한다. 연도별·지역별 시트는 대개 헤더가 같으므로 한 번 매핑하면 전 시트에 적용된다. 헤더가 다른 그룹이 있으면 그 그룹만 별도 매핑 UI를 띄운다.

### 검수 화면

- 검수 목록은 **시트 경계 없이 하나로 합친다.** 항목마다 시트명 배지 + 시트 필터 드롭다운
- 동일 주소 일괄 반영은 **시트를 넘나들어 적용한다.** `'한뫼공원주변'`이 3개 시트 12행에 있으면 한 번 확정으로 12행이 끝난다 (3.4 해소). **동일 여부는 정규화된 원본 주소의 완전일치로 판정한다** — 캐시·사전과 같은 기준이다
- 진행률은 전체 막대 + 시트별 소계

---

## 11. 결과 워크북

### 시트 구성

```
대상지목록          ← 원본. 값·구조 변경 없음
대상지목록_완료      ← 원본 컬럼 + 지오코딩 결과 컬럼
2026신규            ← 원본
2026신규_완료
대체주소사전         ← 이번 실행에서 확정된 사전 사본
처리요약             ← 시트별 성공/실패 집계 + 미해결 목록
```

원본 시트 바로 뒤에 `_완료` 시트를 붙여 대조하기 쉽게 한다. 선택하지 않은 시트는 원본 그대로 통과시킨다.

**시트명 제약**: 엑셀 시트명은 최대 31자다. 원본 시트명이 28자를 넘으면 `_완료`를 붙일 수 없으므로, **앞에서 잘라 `…_완료`가 31자에 맞도록 축약**한다. 축약 후 이름이 충돌하면 뒤에 일련번호를 붙인다. 매핑 결과는 `처리요약` 시트에 기록한다.

**알려진 제약**: `XLSX.writeFile`은 워크북 전체를 다시 쓰므로, 손대지 않은 원본 시트도 **셀 색·글꼴·테두리가 소실된다.** 값과 구조는 보존된다. 서식까지 보존해야 하면 ExcelJS로 교체해야 하며, `excel.js`를 어댑터로 분리해두었으므로 나중에도 교체 가능하다.

### 결과 컬럼

`_완료` 시트에만 추가한다.

| 컬럼 | 내용 |
|---|---|
| `소재지` | 원본 주소 그대로 |
| `대체주소` | **실제로 지오코딩에 성공한 주소.** 원본과 같으면 빈칸 |
| `지번` / `도로명` | API가 돌려준 주소 |
| `X` / `Y` | EPSG:2097 |
| `경도(WGS84)` / `위도(WGS84)` | 소수점 7자리 |
| `매칭방식` | 사전 / 주소검색 / 주소검색(보정) / 장소검색(자동) / 장소검색(선택) / 수동지정 / 기존값 |
| `출처` | kakao / vworld / 사전 |
| `검색어` | 실제 사용한 쿼리 (추적용) |
| `경고` | 게이트 미적용 / 관할 밖 채택 등 |

`대체주소`와 `검색어`를 분리하는 것이 핵심이다 (3.5 해소).

**기존 좌표가 숫자로 채워진 행은 API를 호출하지 않고 원래 값을 유지한다** (현행 동작 유지, `매칭방식: 기존값`).

---

## 12. 좌표계

- **기본값 EPSG:2097 고정.** 실제 사용 데이터로 확인된 좌표계다 (3.6 근거 참조. 최초 설계 당시 5181로 잘못 추정했던 것을 정정)
- VWorld 경로: `crs=EPSG:2097`로 요청 → **변환 없음**
- 카카오 경로: WGS84 반환 → proj4로 2097 변환
- 기존 `자동 판정` 버튼은 **`기존 좌표 검증`으로 성격을 바꾼다.** 기존 좌표가 있는 행을 표본으로 2097 기준 평균 오차를 계산하고, 50m를 넘으면 **경고만** 표시한다. 좌표계를 바꾸는 결정은 사람이 한다
- 좌표계 드롭다운은 남기되 기본 선택은 2097. **드롭다운에서 고른 값은 VWorld 요청의 `crs` 파라미터와 카카오 경로의 proj4 대상 좌표계에 동시에 반영된다.** 두 경로가 서로 다른 좌표계로 기록되는 일이 없어야 한다

---

## 13. 오류 처리

| 상황 | 처리 |
|---|---|
| 연속 API 오류 | **10회 연속이면 전체 중단하고 명시적으로 알린다.** 쿼터 소진이 대량 "결과 없음"으로 위장되는 것을 막는다 (3.7 해소) |
| 일시적 오류 | 지수 백오프 3회 재시도 (현행 유지) |
| VWorld JSONP 무응답 | 요청별 8초 타임아웃. 콜백이 안 오면 오류로 처리 |
| 라이브러리 로드 실패 | `XLSX` / `proj4` / `kakao` 존재 여부를 시작 전 확인. 없으면 시작 버튼을 막고 어느 라이브러리가 안 올라왔는지 안내 (3.10 해소) |
| 사전 파일 형식 오류 | 해당 행만 건너뛰고 몇 행을 무시했는지 보고. 전체를 실패시키지 않는다 |
| 중지 버튼 | 진행 중 작업은 완료시키고 새 작업만 중단. 처리된 부분까지 저장 가능 (현행 유지) |

---

## 14. 테스트

빌드 없이 간다. `test.html`을 브라우저로 열면 케이스가 실행되고 통과/실패가 표시된다.

### 대상 (전부 API·DOM 없이 검증 가능)

| 모듈 | 검증 내용 |
|---|---|
| `address.js` | 정규화(NBSP·연속공백), 행정구역 파싱(2단 시군구, 세종시), 경로 판정, 변형 생성(조합 4종), 키워드 후보 생성 |
| `gate.js` | 관할 내/밖 판정, 표기 차이 흡수, sgg 파싱 실패 시 미적용 |
| `dictionary.js` | 완전일치 조회, miss 처리, 형식 오류 행 건너뛰기 |
| `excel.js` | 헤더 시그니처 그룹화, 컬럼 자동 추정 |

### 필수 회귀 케이스

3장에서 규명한 결함이 되살아나지 않도록 고정한다.

- `'경기도 고양시 일산서구 대화동'` → 주소검색 경로 (지명형 아님)
- `'경기도 고양시 일산서구 한뫼공원주변'` → 키워드 후보에 `'고양시 일산서구 한뫼공원'` 포함
- `'경기도 고양시 일산서구 제1공원'` → 지명형 (숫자가 있어도)
- `'세종특별자치시 조치원읍 …'` → 시군구 파싱 실패 없이 처리
- 관할 밖 결과가 폐기되지 않고 `관할 밖`으로 분류되는지

케이스는 **실제로 실패했던 주소**로 채운다. 이것이 회귀 방지의 핵심이다.

---

## 15. 구현 전 확인이 필요한 사항

### 15.1 API 결과 저장에 관한 약관 — **차단 가능성 있음**

VWorld 지오코더 문서에 다음 문구가 있다.

> "API 요청은 실시간으로 사용하셔야 하며 별도의 저장장치나 데이터베이스에 저장할 수 없습니다"

이 도구는 **좌표를 엑셀에 기록해 시스템 DB에 적재하는 것이 목적**이므로 문구 그대로면 충돌한다. 공공기관 업무용은 협의로 해소되는 경우가 많으니 구현 착수 전 브이월드에 확인해야 한다.

**같은 이유로 카카오도 확인이 필요하다.** 카카오맵 API 약관에도 결과 데이터의 저장·가공에 관한 조항이 있으며, 현행 도구가 이미 그 방식으로 운영 중이다. VWorld 도입 여부와 무관하게 정리해야 할 사안이다.

**저장 제약이 없는 대안** (이번 범위 밖이지만 기록해둔다):
- 행정안전부 주소기반산업지원서비스(`business.juso.go.kr`) — 도로명주소·지번·좌표를 파일 DB로 배포. 구축·저장이 전제이고 인터넷·쿼터가 필요 없다
- WebGISPlatform이 이미 보유한 `jijuk` 테이블 (필지 + `geom`, EPSG:5181). `AddressV2_SQL_Postgres.xml:144`에서 `ST_PointOnSurface(geom)`로 대표점을 뽑고 있다

### 15.2 쿼터

VWorld 지오코더는 **일 40,000건**이다 (카카오 300,000건 대비 1/7). 1회 처리 건수가 이를 넘는지 확인해야 한다. 넘는다면 VWorld는 정형 주소 전용으로 두고 지명형은 카카오로 보내는 배분이 필요하다.

### 15.3 실제 데이터 확인

행정구역 파싱 규칙은 실제 엑셀을 봐야 확정된다. 특히:
- 지명형 주소가 전체의 몇 %인지
- `sgg`가 단일 시·군인지 여러 시·도에 걸치는지
- 파싱 규칙의 예외 케이스 (세종시, 2단 시군구 외)

### 15.4 셀 서식

11장의 제약대로 SheetJS로는 서식이 소실된다. 원본에 색·조건부서식이 있고 그것이 업무상 의미를 가진다면 ExcelJS 교체를 재검토해야 한다.

---

## 16. 권장 구현 순서

각 단계는 독립적으로 검증 가능하다. 앞 단계가 끝나면 그 시점에도 도구가 동작해야 한다.

1. **`address.js` + `gate.js` + `test.html`** — 파싱·변형·게이트를 순수 함수로 작성하고 회귀 케이스를 고정한다. API 없이 검증되므로 여기서 로직을 확정한다 (3.1 / 3.2 해소)
2. **기존 `app.js`에 1단계를 배선** — 프로바이더는 아직 카카오만. 이 시점에 오답 차단 효과가 바로 나온다 (3.3 해소)
3. **출력 컬럼 개편 + 좌표계 2097 고정** (3.5 / 3.6 해소)
4. **오류 처리** — 연속 실패 차단기, 캐시 정책, 라이브러리 로드 확인 (3.7 / 3.8 / 3.10 해소)
5. **`dictionary.js`** — 사전 불러오기/조회/내보내기
6. **다중 시트** — 데이터 모델을 `sheets[]` 배열로 전환. 동일 주소 일괄 반영 포함 (3.4 해소). 구조 변경이 가장 크므로 앞 단계가 안정된 뒤에 한다
7. **결과 워크북 재구성** — `A` / `A_완료` 인터리브
8. **VWorld 프로바이더** — 15.1의 약관 확인이 끝난 뒤에만 착수한다

---

## 17. 참고

- [VWorld Geocoder API 2.0 레퍼런스](https://www.vworld.kr/dev/v4dv_geocoderguide2_s001.do)
- [VWorld 검색 API 2.0 레퍼런스](https://vworld.kr/dev/v4dv_search2_s001.do)
- [sheetjs#3214 — cellStyles 미보존](https://git.sheetjs.com/sheetjs/sheetjs/issues/3214)
- [ExcelJS](https://github.com/exceljs/exceljs)
외부 참조 — **별도 저장소 WebGISPlatform**. 이 도구의 의존 대상이 아니다. 최초 설계 당시 좌표계 근거(현재는 실데이터 확인으로 EPSG:2097로 정정됨)와 향후 내부 DB 대안(15.1)을 확인한 출처다.

- `.../sqlmap/mapper/component/map/AddressV2_SQL_Postgres.xml` — `jijuk` 필지 테이블, `ST_PointOnSurface(geom)`
- `.../sqlmap/mapper/component/map/MapAnalyse_SQL_Postgres.xml` — `ST_SetSRID(..., 5181)`
